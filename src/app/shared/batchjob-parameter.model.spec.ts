import { BatchjobParameter } from './batchjob-parameter.model';

describe('BatchjobParameter', () => {
  it('should create an instance', () => {
    expect(new BatchjobParameter()).toBeTruthy();
  });
});
